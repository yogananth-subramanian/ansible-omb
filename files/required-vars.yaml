---
required_vars:
  - VAULT_OCM_TOKEN
  - VAULT_RHOAS_QUAY_TOKEN
  - VAULT_BF2_QUAY_TOKEN
  - VAULT_BF2_BOT_TOKEN
  - VAULT_REDHAT_SSO_CLIENT_ID
  - VAULT_REDHAT_SSO_CLIENT_SECRET
  - VAULT_AWS_ACCESS_KEY_ID
  - VAULT_AWS_ACCOUNT_ID
  - VAULT_AWS_SECRET_ACCESS_KEY
  - VAULT_CLOUD_USER
  - NUMBER_OF_KAFKA_INSTANCES
  - KAFKA_WORKER_COUNT
  - KAFKA_MACHINE_TYPE
  - KAFKA_CAPACITY_INGRESS_THROUGHPUT
  - KAFKA_CAPACITY_EGRESS_THROUGHPUT
  - KAFKA_CAPACITY_TOTAL_MAX_CONNECTIONS
  - KAFKA_CAPACITY_MAX_DATA_RETENTION_SIZE
  - KAFKA_CAPACITY_MAX_PARTITIONS
  - KAFKA_CAPACITY_MAX_DATA_RETENTION_PERIOD
  - KAFKA_CAPACITY_MAX_CONNECTION_ATTEMPTS_PER_SEC
  - OMB_MACHINE_TYPE
  - OMB_TEST_DURATION
  - TARGET_RATE
  - WORKERS_PER_INSTANCE
  - TOPICS_PER_KAFKA
  - PRODUCERS_PER_TOPIC
  - PAYLOAD_FILE_SIZE
  - PARTITIONS_PER_TOPIC
  - SUBSCRIPTIONS_PER_TOPIC
  - CONSUMER_PER_SUBSCRIPTION
  - KAS_INSTALLER_TAG
  - COMPUTE_MACHINE_TYPE
  - WORKER_COUNT_MIN
  - WORKER_COUNT_MAX
  - KAFKA_MIN_WORKER_COUNT
  - KAFKA_MAX_WORKER_COUNT
  - OPENSHIFT_VERSION
  - cluster_name
  - cluster
